server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ =404;
    }

    # Automaticaly redirects any 404 Not Found error to the Homepage
    error_page 404 =301 /;
}
